name: fe
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: any
  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  # 기본 Flutter 패키지
  cupertino_icons: ^1.0.2
  shared_preferences: ^2.0.15

  # 상태 관리 및 데이터 처리
  flutter_riverpod: ^2.4.3
  riverpod_annotation: ^2.3.3
  freezed_annotation: ^2.4.1
  json_annotation: ^4.8.1
  # Hooks
  hooks_riverpod: ^2.4.9
  flutter_hooks: ^0.20.4
  # 네트워크 및 API 통신
  retrofit: ">=4.0.0 <5.0.0"
  dio: ^5.4.3+1
  native_dio_adapter: ^1.3.0
  http: ^1.2.1

  google_sign_in: ^6.1.5
  sign_in_with_apple: ^6.1.0
  kakao_flutter_sdk_user: ^1.6.1
  # 로컬 저장 및 보안
  flutter_secure_storage: ^9.0.0
  crypto: ^3.0.3
  uuid: ^4.0.0
  # 네비게이션 및 라우팅
  go_router: ^14.0.2
  uni_links: ^0.5.1
  # UI 및 디자인
  lottie: ^3.1.0
  flutter_svg: ^2.0.9
  fl_chart: ^0.68.0
  material_color_utilities: ^0.11.0
  mailer: ^6.2.0

  fluttertoast: ^8.2.4
  auto_size_text: ^3.0.0
  date_picker_plus: ^4.0.0
  flutter_sticky_header: ^0.6.5
  carousel_slider: ^4.2.1
  # 디바이스 정보 및 기능
  device_info_plus: ^10.1.0
  device_region: ^1.0.1
  android_id: ^0.3.6
  # 알림 및 알람 기능
  flutter_local_notifications: ^17.1.0
  alarm: ^3.0.8
  # timezone 및 날짜 관련
  timezone: ^0.9.2
  flutter_timezone: ^1.0.8
  # 공유하기
  share_plus: ^10.1.1
  # 이미지
  image_picker: ^1.1.1
  exif: ^3.3.0
  # 지도
  google_maps_flutter: ^2.9.0
  sendbird_chat_sdk: ^4.2.16
  # 웹뷰
  webview_flutter: ^4.8.0
  flutter_html: ^3.0.0-beta.2
  url_launcher: ^6.3.0
  # 기타 기능
  logger: ^2.0.2+1
  tuple: ^2.0.2
  flutter_dotenv: ^5.1.0
  sentry_flutter: ^8.0.0
  flutter_app_badger: ^1.5.0
  package_info_plus: ^8.0.0
  pretty_dio_logger: ^1.3.1
  path_provider: ^2.1.3
  qr_code_scanner: ^1.0.1
  flutter_naver_login: ^2.0.0
  permission_handler: ^11.3.1
  riverpod: ^2.5.1
  qr_flutter: ^4.1.0
  flutter_inappwebview: ^6.0.0
  geolocator: 12.0.0
  firebase_core: ^3.6.0
  firebase_database: ^11.1.4
  firebase_messaging: ^15.1.3

dev_dependencies:
  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^4.0.0
  custom_lint: ^0.6.4
  riverpod_lint: ^2.1.0
  build_runner: ^2.4.7
  retrofit_generator: ^8.1.0
  riverpod_generator: ^2.3.9
  json_serializable: ^6.7.1
  freezed: ^2.4.6
  intl_utils: ^2.8.7
  sentry_dart_plugin: ^1.0.0

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true
  assets:
    - assets/images/
    - assets/images/svg/
    - assets/images/profile/
  fonts:
    - family: nanumSquare
      fonts:
        - asset: assets/fonts/NanumSquareNeo-Regular.woff
        - asset: assets/fonts/NanumSquareNeo-Regular.woff2
          weight: 400
        - asset: assets/fonts/NanumSquareNeo-Bold.woff
        - asset: assets/fonts/NanumSquareNeo-Bold.woff2
          weight: 700
        - asset: assets/fonts/NanumSquareNeo-ExtraBold.woff
        - asset: assets/fonts/NanumSquareNeo-ExtraBold.woff2
          weight: 800
    - family: Roboto
      fonts:
        - asset: assets/fonts/Roboto-Light.woff
        - asset: assets/fonts/Roboto-Light.woff2
          weight: 400
        - asset: assets/fonts/Roboto-Regular.woff
        - asset: assets/fonts/Roboto-Regular.woff2
          weight: 700
        - asset: assets/fonts/Roboto-Bold.woff
        - asset: assets/fonts/Roboto-Bold.woff2
          weight: 800
    - family: SanFranciscoPro
      fonts:
        - asset: assets/fonts/SanFranciscoPro-Thin.woff2
          weight: 400
        - asset: assets/fonts/SanFranciscoPro-Regular.woff2
          weight: 700
        - asset: assets/fonts/SanFranciscoPro-Bold.woff2
          weight: 800


  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/assets-and-images/#resolution-aware

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/assets-and-images/#from-packages

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/custom-fonts/#from-packages
